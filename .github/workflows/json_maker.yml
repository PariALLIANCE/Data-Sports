name: Football Scraper Daily1

on:
  # Exécution automatique tous les jours à 01h UTC
  # schedule:
  #   - cron: '0 1 * * *'

  workflow_dispatch:

jobs:
  scrape-football:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Récupérer le repo (IMPORTANT : fetch-depth 0)
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2️⃣ Installer Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      # 3️⃣ Installer les dépendances
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install requests beautifulsoup4

      # 4️⃣ Exécuter le script
      - name: Run football scraper
        run: python l.py

      # 5️⃣ Commit et push (CORRIGÉ)
      - name: Commit and push results
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"

          git pull --rebase origin main

          git add data/football/leagues/*.json
          git commit -m "Update football JSONs [ci skip]" || echo "No changes to commit"
          git push origin main