name: Generate Dataset (Manual)
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install dependencies
        run: pip install --upgrade pip

      - name: 🔧 Configure git
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

      - name: ▶️ Run dataset script
        run: python scripts/build_dataset.py

      - name: 💾 Commit and push dataset
        run: |
          git pull --rebase
          git add dataset_ml1.json dataset_ml2.json dataset_ml3.json dataset_ml4.json
          git diff --cached --quiet && echo "Rien à committer" && exit 0
          git commit -m "🤖 Update dataset_ml [$(date +'%Y-%m-%d %H:%M')]"
          git push
